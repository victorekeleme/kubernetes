apiVersion: v1
kind: Service
metadata:
  name: serviceName
  namespace: serviceNamespace
spec:
  selector:
    app: app-name
  clusterIP: None
  ports:
  - name: nameService
    protocol: TCP
    port: portNumber
    targetPort: containerPort